# Project state

## 2026-08-10 — новый production VPS и HTTPS

- Production работает на `185.65.200.69`: systemd `complexmedia` и Nginx активны.
- Для `complexmedia.ru` выпущен и установлен Let's Encrypt сертификат; HTTPS отвечает `200`, HTTP перенаправляется на HTTPS.
- Сертификат действителен до 2026-11-08 и настроено автоматическое продление Certbot.
- `www.complexmedia.ru` пока указывает на старый VPS `45.67.32.233`; DNS-запись не менялась.

## 2026-08-11 — локальная SEO-услуга голосового ИИ

- Локально добавлена `/services/voice-ai-consultant`: metadata, Service schema, телефонная демонстрация, CTA и сценарии внедрения.
- Страница добавлена в главную, desktop/mobile-меню, ServiceNavigation и sitemap; в SEO-дашборд добавлены 6 коммерческих запросов.
- TypeScript проходит; production build компилируется и генерирует 33 маршрута. Финальный standalone-шаг локально блокируется Windows symlink от pnpm.
- Новая услуга пока не развернута в production и не отправлена на переобход.
- Для страницы добавлен видимый FAQ и `FAQPage` schema; sitemap получил актуальную дату `2026-08-11`.
- Read-only production-проверка: корень и live sitemap отвечают `200`, новый URL пока `404` до deploy.
- Публичные DNS `1.1.1.1` и `8.8.8.8` ещё возвращают старый IP для `www`; отдельный сертификат `www` не выпускался.

## 2026-08-11 — production SEO deploy

- Production обновлён до `874c853`: `/services/voice-ai-consultant` и ссылка из
  `/cases/cosmetology-clinic` отвечают `200`; sitemap содержит 22 URL.
- Яндекс Вебмастер принял URL голосовой услуги в очередь переобхода; статус
  `IN_PROGRESS`, task id сохранён только в локальном журнале текущей работы.
- Google переобход не запускался: доступ сервисного аккаунта к Search Console
  остаётся read-only, предыдущая отправка sitemap возвращала `403`.

Обновлено: 2026-07-30

- Публичный SEO-пакет без `/seo-dashboard`, middleware и OAuth-файлов
  зафиксирован коммитом `8641328` и отправлен в `origin/master`.
- Production автоматически не обновился: новые `/articles` и
  `/services/ai-sales-automation` пока отвечают `404`. Для deploy по-прежнему
  нужен рабочий SSH-доступ к `45.67.32.233`.

## Local

- Репозиторий клонирован в `D:\yu\Codex\complexmedia site`.
- Активна чистая ветка `master`, tracking `origin/master`.
- HEAD: `c676a64` — `Fix resend route and favicon`.
- Ponytail Codex-плагин уже установлен; режим `full` активен.
- Локальные незакоммиченные изменения:
  - Resend заменён на Nodemailer с прямым SSH/sendmail-транспортом;
  - все ссылки на `https://t.me/complexmedia_bot` и связанные CTA удалены;
  - Яндекс Метрика загружается только после явного согласия; добавлен баннер
    с вариантами «Отклонить» и «Разрешить»;
  - политика конфиденциальности обновлена с учётом Метрики и cookie;
  - на главную добавлены пять кейсов автоматизации и блок предварительного
    бизнес-аудита;
  - созданы страницы кейсов и полноэкранные статические интерфейсы для
    скриншотов: клиника, металлопрокат, логистика, B2B-оборудование и личный
    ИИ-помощник руководителя;
  - на странице ИИ-агентов добавлены возможности личного помощника руководителя;
  - кейсы уточнены: клиника работает 24/7 с параллельными диалогами, поиск
    регламента в металлопрокате — `~30 сек.`, в кейс помощника аккуратно добавлены
    AI Harness и Hermes, бизнес-анализ индивидуализирован;
  - добавлены уникальные title/description, canonical, Open Graph/Twitter,
    Organization/WebSite и BreadcrumbList; Sitemap содержит все публичные
    страницы и кейсы;
  - удалён конфликтующий `public/favicon.ico`; favicon App Router теперь отдаётся
    без ошибки 500.
- TypeScript, ESLint, `git diff --check`, HTML-метаданные, H1, внутренние ссылки,
  Sitemap и 13 публичных маршрутов проверены локально.
- Preview доступен на `http://127.0.0.1:3001`.

## Production

- Сайт работает: Nginx → PM2 `complexmedia` → `.next/standalone/server.js`, порт `3000`.
- VPS checkout: `/var/www/complexmedia`, ветка `master`, HEAD `c676a64`.
- 2026-07-30 все локально согласованные изменения задеплоены:
  - новая отправка формы через выделенный SSH/sendmail;
  - удаление ссылок на Telegram-бота;
  - consent для Яндекс Метрики и обновлённая политика;
  - пять кейсов, демо-интерфейсы и блоки бизнес-анализа;
  - SEO metadata, structured data, robots и Sitemap.
- PM2 `complexmedia` после deploy находится в статусе `online`.
- Production health-check: все публичные маршруты, Sitemap и favicon возвращают
  `200`; demo возвращает `noindex, nofollow`.
- Реальная техническая заявка через `https://complexmedia.ru/api/send-email`
  вернула `200`.
- Почтовый ключ установлен в
  `/home/ubuntu/.ssh/complexmedia_forn_mail_vps_ed25519` с mode `600`;
  SSH-подключение к mail VPS проверено.
- Резервная копия до переключения:
  `/home/ubuntu/complexmedia-backup-20260730-095742.tar.gz`.
- На VPS установлен Node `20.19.6`; фактически в `node_modules` Next.js `16.0.7`, тогда как `package.json` фиксирует `15.3.1`.
- Production checkout не чистый:
  - изменены `next.config.ts`, `tsconfig.json`;
  - не отслеживаются `ecosystem.config.js` и три файла в `public/`.
- Эти изменения не копировались, не коммитились и не перезаписывались.
- Production `public/favicon.ico` сохранён как отдельный Nginx-ассет: правило
  Nginx обслуживает `.ico` напрямую, тогда как локально используется
  `src/app/favicon.ico`.

## Архитектура

- `src/app` — страницы, metadata, sitemap и API `POST /api/send-email`.
- `src/components` — Header/Footer, мобильное меню, форма, анимации и секции главной.
- `src/emails` — React Email-шаблон заявки.
- `public` — логотипы, favicon, OG-изображения и production-only материалы.
- Контактная форма валидируется Zod и через системный `ssh` передаёт письмо штатному `sendmail` на mail VPS.

## Риски

- Default-ветка GitHub `main` не соответствует production; работать нужно с `master`.
- Production содержит незакоммиченные файлы, поэтому обычный `git pull`/deploy без сверки опасен.
- В production `ecosystem.config.js` хранится API-ключ открытым текстом; ключ нужно ротировать и перенести в защищённое окружение.
- Версия Next.js в production `node_modules` не совпадает с manifest/lockfile.
- Выделенный пользователь `complexmedia_forn` и SSH-ключ работают; sudo отсутствует.
- Техническое письмо через `/usr/sbin/sendmail` принято без остатка в очереди.
- Локальный `POST /api/send-email` после исправления отвечает `200`; письмо уходит через тот же маршрут.
- Выделенный mail-ключ скопирован только на production VPS сайта и защищён
  правами `600`.

## SEO и индексация

- 2026-07-30 собрана частотность Яндекс Wordstat по ИИ-услугам за период
  29.06.2026–28.07.2026; кластеры и карта страниц сохранены в
  `SEO_SEMANTIC_MAP.md`.
- Главная и страницы услуг с обновлённой семантикой задеплоены в production:
  «внедрение ИИ в бизнес», «ИИ-чат-боты для бизнеса»,
  «разработка ИИ-агентов для бизнеса» и «корпоративная база знаний».
- CTA конкретизированы по услуге: заказать ИИ-чат-бота, ИИ-агента или
  корпоративную базу знаний; остальные коммерческие страницы сохраняют
  собственный CTA.
- Нулевые запросы не вынесены в title/H1: «заказать корпоративную базу
  знаний» используется как CTA, а не как основная семантика.
- Сайт добавлен и подтверждён в Яндекс Вебмастере и Google Search Console;
  `https://complexmedia.ru/sitemap.xml` отправлен в обе системы.
- В Яндекс Вебмастере на переобход поставлены главная и четыре страницы услуг.
- Файл `public/yandex_580a296a69f9c90a.html` сохранять в production: он
  подтверждает права на сайт.
- В Яндекс Метрике создана цель «Успешная отправка формы» с идентификатором
  `contact_form_success`; автоцели форм и переходов в мессенджеры уже включены.
- Локально добавлена отдельная содержательная страница
  `/services/ai-sales-automation` под кластер «автоматизация отдела продаж с
  ИИ»; она добавлена в меню услуг, главную, форму и sitemap.
- В desktop-шапке локально добавлено нативное выпадающее меню всех услуг; в
  мобильном меню добавлены прямые ссылки, включая «ИИ для продаж».
- Локально добавлена `/services/executive-ai-assistant` под среднечастотные
  запросы «ИИ-ассистент руководителя» и «ИИ-помощник руководителя»; страница
  связана с главной, ИИ-агентами, кейсом, меню, формой и sitemap.
- Локально добавлена `/services/ai-consulting` под запрос «ИИ-консалтинг»;
  страница описывает карту процессов, приоритеты, готовность данных, экономику
  пилота, риски и дорожную карту и связана с блоком бизнес-аудита на главной.
- Локально добавлено руководство
  `/articles/ai-implementation-business`: выбор первого процесса, этапы,
  факторы стоимости, KPI пилота, ошибки и FAQ. Добавлены Article, FAQPage и
  BreadcrumbList schema, ссылки с главной, ИИ-консалтинга и Footer.
- TypeScript, ESLint, `git diff --check` и production build Next.js 15.3.1
  проходят. Локальный preview обновлён на `http://127.0.0.1:3001`.
- Новые страницы продаж, помощника и ИИ-консалтинга, SEO-руководство, меню услуг
  и событие формы ещё не деплоились: сначала нужен просмотр пользователем и
  отдельное разрешение на production deploy.
- Локально добавлена статья `/articles/ai-for-sales` под кластер «ИИ для
  отдела продаж»: сценарии, интеграции, роли ИИ-бота/агента/базы знаний, KPI
  пилота и FAQ. Добавлены Article, FAQPage и BreadcrumbList schema,
  контекстная ссылка со страницы услуги, ссылка в Footer и запись в sitemap.
- Для `/articles/ai-for-sales` проходят TypeScript, ESLint, `git diff --check`,
  локальный `200`, schema/sitemap и desktop/mobile-проверка без переполнения.
  Production не менялся.
- Локально добавлена `/articles/corporate-knowledge-base`: подготовка
  документов, структура, права и версии, RAG/ИИ-поиск, KPI пилота и FAQ.
  Статья связана с услугой `/services/digital-asset`, кейсом производства,
  Footer и sitemap; добавлены Article, FAQPage и BreadcrumbList schema.
- TypeScript, ESLint, `git diff --check`, локальные `200`, schema/sitemap,
  desktop/mobile и консоль проходят. Production не менялся.
- Локально добавлена `/articles/ai-agent-vs-chatbot`: сравнение по ролям,
  самостоятельности и интеграциям, совместный сценарий, безопасный пилот и
  FAQ. Статья связана с услугами ИИ-ботов и ИИ-агентов, двумя кейсами, Footer
  и sitemap; добавлены Article, FAQPage и BreadcrumbList schema.
- TypeScript, ESLint, `git diff --check`, локальные `200`, перелинковка,
  schema/sitemap, desktop/mobile и консоль проходят. Production не менялся.
- Локально добавлен индекс `/articles` с четырьмя SEO-материалами, CTA и
  ItemList schema. Ссылка «Статьи» добавлена в Header, MobileMenu и Footer,
  маршрут — в sitemap.
- Раздел проверен на ширинах 1280, 900, 768 и 390 px; desktop/mobile-меню,
  четыре карточки, CTA, отсутствие переполнения и консоль проходят.
  Production не менялся.
- Финальный production build Next.js 15.3.1 успешно собрал 32 страницы.
- Все 21 индексируемый URL из sitemap отвечают `200`, содержат один H1 и один
  canonical.
- В Яндекс Вебмастер добавлены 30 целевых запросов для мониторинга позиций,
  показов, кликов и CTR. Сейчас данные по ним нулевые и ещё накапливаются.
- Google Search Console сообщает, что данные обрабатываются и предлагает
  повторить проверку примерно через день.
- Baseline Метрики за 1–30 июля: 170 визитов, 129 посетителей, 3 перехода из
  поисковых систем. Прямые ссылки и регламент сохранены в `SEO_MONITORING.md`.
- Контентный план 4/4 завершён; новые статьи до появления фактических запросов
  с показами не создаются, чтобы не конкурировать со страницами услуг.
- Локально добавлен `/seo-dashboard`: baseline Метрики, каналы трафика, 30
  отслеживаемых запросов, таблица динамики и ссылки на официальные отчёты.
- Дашборд закрыт HTTP Basic Auth через `SEO_DASHBOARD_USER` и
  `SEO_DASHBOARD_PASSWORD`, имеет `noindex, nofollow` и отсутствует в публичном
  меню и sitemap. Без переменных окружения маршрут отвечает `503`.
- Финальный production build после добавления дашборда и middleware успешно
  собрал 33 страницы. Проверено: без авторизации `401`, с авторизацией `200`,
  desktop/mobile без горизонтального переполнения.
- Production-деплой текущего SEO-пакета не выполнен: web VPS отклонил прежний
  пароль. После двух попыток дальнейшие подключения остановлены до получения
  актуального пароля или SSH-ключа.
- В дашборде период выбирается календарями. Добавлена таблица всех 30 запросов
  с позициями Google/Яндекса и изменениями за
  день/неделю; сейчас значения пусты, поскольку Google обрабатывает данные, а
  Яндекс ещё не передал позиции.
- Диагностика SSH без ввода пароля подтвердила: web VPS доступен и предлагает
  методы `publickey,password`. Парольный вход на сервере не отключён.
- Google Cloud: создан отдельный проект `complexmedia-seo-dashboard`, включён
  Search Console API, сервисному аккаунту выдан ограниченный доступ к ресурсу.
  Локальный вызов API успешен; данных по новому ресурсу пока нет.
- Яндекс OAuth-приложение создано с правами чтения Вебмастера и Метрики.
  Для завершения локального подключения нужен одноразовый безопасный обмен кода
  на `YANDEX_OAUTH_TOKEN`; токен и Client secret в репозиторий не добавлялись.
- Ядро ИИ-чат-ботов усилено коммерческими формулировками «заказать», «под ключ»
  и «стоимость»; неоднозначные широкие запросы исключены из KPI.
- 01.08.2026: публичный SEO-пакет уже находится в `origin/master` (`8641328`),
  но production пока работает со старой версией.
- Защищённый `/seo-dashboard` завершён: выбор периода, Метрика, источники,
  позиции и дневная/недельная динамика Google/Яндекс, реальные новые запросы.
  Google Search Console и Метрика отвечают; Яндекс Вебмастер требует новый токен
  с правом `webmaster:verify`, его сохранение ожидает отдельного подтверждения.
- Подготовлен GitHub Actions deploy с lock, fast-forward merge, production build,
  PM2 health-check и сохранением production-фавикона. Для запуска нужен рабочий
  SSH deploy-key: прежняя парольная авторизация VPS продолжает отклоняться.
- 05.08.2026: пользователь разрешил `webmaster:verify`; новый OAuth-токен
  выпущен и сохранён вне Git с ACL только для текущего пользователя.
- Все источники дашборда отвечают: Google Search Console, Яндекс Вебмастер и
  Метрика. За 20.07–05.08 Яндекс вернул 9 фактических запросов с позициями,
  Google — 2 брендовых запроса; целевые коммерческие запросы показов ещё не дали.
- Deploy-key на web VPS по-прежнему не установлен: read-only SSH-проверка
  возвращает `Permission denied`, новые production-маршруты остаются заблокированы.
- 06.08.2026: ручной production deploy завершён на `fed4049`. Главная, статьи,
  новые услуги и sitemap отвечают `200`; старые Telegram-ссылки отсутствуют.
- Production `/seo-dashboard` закрыт Basic Auth и отвечает `200`; Google Search
  Console, Яндекс Вебмастер и Метрика показывают `API подключён`. Production
  credentials находятся вне Git в `/home/ubuntu/.config/complexmedia`.
- Яндекс API: статус сайта `OK`, в поиске 8 URL, из них 4 старые страницы услуг;
  новые статьи и новые услуги пока не появились. Google Search Console за
  08.07–06.08 не вернул показов по `/articles` и новым `/services`.
- GitHub Actions workflow пока пропускает SSH deploy, потому что секрет
  `DEPLOY_SSH_KEY` ещё не добавлен; ручной deploy работает.
## Актуализация 06.08.2026

- `complex media` и `ооо "комплекс медиа"` исключены из KPI-дашборда; коммерческое ядро сохранено.
- Усилены коммерческие title/description и H1/CTA страниц ИИ-агента, автоматизации продаж с ИИ, корпоративной базы знаний и личного ИИ-помощника.
- Яндекс принял 14 URL в очередь переобхода; sitemap уже зарегистрирован, приоритетный переобход ожидает обработки.
- Google API вернул 403 на отправку sitemap: текущему сервисному аккаунту выдан read-only доступ.
## Проверка 08.08.2026

- Яндекс: 21 URL в индексе, очередь переобхода завершена.
- Google: за последние 30 дней 2 брендовых строки, коммерческих показов нет.
- В SEO-дашборд добавлены пресеты последнего дня и последних 3 дней.

## 2026-08-10 — production incident and recovery

- Причина недоступности: эксплуатация уязвимой версии Next.js 15.3.1 через React Server Components; после POST-запросов на приложение запускались сторонние процессы, из-за чего CPU и память VPS исчерпывались. Перезагрузка временно убирала процессы, но не устраняла уязвимость.
- Выполнено: удалены подтверждённые cron/profile persistence-записи, вредоносные файлы помещены в `/home/ubuntu/complexmedia-incident-20260810`, остановлены посторонние процессы, выполнены чистые `npm ci` и production build.
- Next.js и `eslint-config-next` обновлены до 15.5.21; опубликован commit `0b3c348`. HTTPS и локальная проверка возвращают `200`, PM2 online.
- Требуется после компрометации: ротация всех секретов, доступных пользователю `ubuntu`, и плановый rebuild VPS из чистого образа. Диск заполнен на 94% (около 635 MB свободно).
